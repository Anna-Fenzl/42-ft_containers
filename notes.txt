
> MAKFILE RELINKS RN


> throw () is an exception specifier that declares that a this funciton will never throw an exception
	iterator
		end () throw()	{ return iterator(_end); }

has a problem when using a copied map

def.erase(++def.begin(), def.end());
		def.print();
erases whole tree
--> ++ operators fucked

insert.erase(--insert.end());
runs endlessly in rbt_max